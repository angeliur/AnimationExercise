<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.angeliur.animation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".launchmode.ActivityA" android:launchMode="standard"/>
        <activity android:name=".launchmode.ActivityB" android:launchMode="singleTop"/>
        <activity android:name=".launchmode.ActivityC" android:launchMode="singleTask"/>
        <activity android:name=".launchmode.ActivityD" android:launchMode="singleInstance"/>

        <activity android:name=".anim.AnimTestActivity"/>
        <activity android:name=".anim.DropTest"/>
        <activity android:name=".anim.PropertyTest"/>
        <activity android:name=".anim.TimerTest"/>

        <activity android:name=".viewanim.ViewAnimTestActivity"/>
        <activity android:name=".vector.VectorTestActivity"/>

        <activity android:name=".customanim.CustomAnimTestActivity"/>
    </application>

</manifest>